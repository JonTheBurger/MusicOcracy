<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.musicocracy.fpgk.ui">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.musicocracy.fpgk.CyberJukeboxApplication">
        <!-- We're going to use our custom application class CyberJukeboxApplication -->

        <activity android:name="com.musicocracy.fpgk.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.musicocracy.fpgk.ui.BrowseActivity" />
        <activity android:name="com.musicocracy.fpgk.ui.DatabaseActivity" />
        <activity
            android:name="com.musicocracy.fpgk.ui.BroadcastReceiverActivity"
            android:launchMode="standard" />
        <activity android:name="com.musicocracy.fpgk.ui.BroadcastSenderActivity" />
        <activity android:name="com.musicocracy.fpgk.ui.AuthenticationActivity" />

        <!-- Needed for LoginActivity to work -->
        <activity
            android:name="com.spotify.sdk.android.authentication.LoginActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name="com.musicocracy.fpgk.ui.NetworkTestActivity"></activity>
    </application>

</manifest>