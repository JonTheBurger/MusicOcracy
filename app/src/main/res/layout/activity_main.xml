<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.musicocracy.fpgk.musicocracy.MainActivity">

    <Button
        android:text="@string/button_send"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/sender_button"
        android:onClick="gotoBroadcastSender"
        android:layout_marginTop="80dp"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@+id/button_receive"
        android:layout_alignStart="@+id/button_receive" />

    <Button
        android:text="@string/receive"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/button_receive"
        android:onClick="gotoBroadcastReceiver"
        android:layout_marginTop="20dp"
        android:layout_above="@+id/browse_button"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="@string/browse"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/browse_button"
        android:onClick="launchBrowseActivity"
        android:layout_marginTop="20dp"
        android:layout_centerVertical="true"
        android:layout_alignLeft="@+id/button_receive"
        android:layout_alignStart="@+id/button_receive" />

    <Button
        android:text="Launch Database Activity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/database_button"
        android:onClick="launchDbActivity"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/browse_button"
        android:layout_centerHorizontal="true" />

</LinearLayout>